# Part 12 — Difference Report

Original: `docs/design/aura/DESIGN.md` (126 lines, unmodified).
Verified: `docs/design/aura/DESIGN_VERIFIED.md`.

| # | Original section | Identified issue | Verified replacement | Reason | Product evidence | Implementation impact |
|---|---|---|---|---|---|---|
| 1 | frontmatter `colors.primary-hover` | `#991B12` is lighter than the brand-approved "#7F1D1D or equivalent" dark red | Kept as-authored, flagged `# CONFIRM WITH OWNER` inline | This is a brand-fidelity question, not a factual error — should not be silently changed without sign-off | Brand brief in mission Part 2 | None until owner confirms; if changed later, only a CSS variable value updates |
| 2 | body `## Colors`, "Blue for standard focus" | Undefined hex value for an implied focus-indicator color | Added `focus-ring: "#2563EB"` token + `focus-outline` component | Undefined tokens force implementers to guess; guessing risks an inaccessible or off-brand color | Part 1 Important #1, Part 2 Important #2 | Low — one new CSS variable, applied via `:focus-visible` |
| 3 | frontmatter `typography.h2` | Missing `lineHeight` | Added `lineHeight: "1.15"` | h1's 1.05 is too tight for 36px text and risks descender clipping if reused | Part 1 Important #5 | Trivial — one CSS property |
| 4 | body `## Page Sections → Intelligence Feature Blocks`, "Intelligent Service Advisor" (dark section) | No foreground text color defined for the `#171717` dark background; literal reuse of `text-main` (also `#171717`) would be invisible | Added `text-on-dark: "#FAFAFA"` token, called out explicitly wherever the dark section is described | Following the original spec literally on this point would ship illegible text | Part 1 Important #4 (via Part 2's dark-mode finding) | Low — one new token, applied to one section |
| 5 | frontmatter `colors.text-light` (#A3A3A3) | 2.42:1 contrast on `#FAFAFA` — fails WCAG AA at every text size; original spec never restricted its use | Restricted to decorative/non-text use only, documented inline and in Do's/Don'ts | Prevents an accessibility failure that the original spec's silence made likely | Part 10 FAIL finding (computed contrast ratio) | None if never used as text; requires a design-review checkpoint if a future contributor reaches for this token for copy |
| 6 | frontmatter `colors.success` / `colors.warning` | Both fail AA as small text on light backgrounds (3.77:1 and 2.15:1 respectively) | Added inline usage restrictions (large-text/icon/badge only for success; dark-section/badge-only for warning) | Colors themselves are fine (and warning already appears only in the dark section in the original); the fix is a usage rule, not a color change | Part 10 computed ratios | None — same hex values, added guidance |
| 7 | body `## Hero Section`, "RedlineD1 Engine v2.0" pill | No versioned "Engine" concept exists anywhere in the actual product/codebase | Removed; replaced with guidance to use a truthful, non-versioned badge if desired | Fabricated version marker reads as an unsupported technical-maturity claim to visitors | Part 3 UNSUPPORTED finding (repo-wide search found zero matches outside the DESIGN.md files themselves) | Removes one hero badge element; no functional loss, avoids a false claim |
| 8 | body `## Intelligence Feature Blocks`, "Owner Intelligence" — "$12,450 Stale Estimates" | Specific, precise-looking dollar figure with no evidentiary backing, easily mistaken for a real customer statistic | Reworded to an explicitly-labeled illustrative example ("$X,XXX ... illustrative example" or a "Sample data" badge) | Mission's rule against exact performance/revenue claims without evidence | Part 3 NEEDS REWORDING finding | Copy change only; no visual redesign needed |
| 9 | body `## Intelligence Feature Blocks`, "Owner Intelligence" (maturity) | Implies a fully-built decision dashboard | Labeled "Available Now (core), Rolling Out (full decision-dashboard scoring)" | `CLAUDE.md`'s own "Next Recommended Phase" (SI-5) states the Command Center is still being transformed into a decision dashboard and that work has not yet been instructed to start | Part 3 PARTIAL finding, `CLAUDE.md` SI-5 section | Sets correct expectations; no visual change |
| 10 | body `## Motion & Interaction`, "Aura Controls" sentence | Ambiguous — reads as if describing an existing RedlineD1 feature when it actually describes implementation guidance for whoever builds the page | Reworded to explicitly frame it as a build instruction, not a shipped feature | Prevents a future reader from citing this as a product claim | Part 3 NEEDS REWORDING finding | None — documentation clarity only |
| 11 | body `## Assets` | CDN Tailwind Play script (`cdn.tailwindcss.com`) | Removed; replaced with "local CSS / CSS variables, no Tailwind installed" guidance | Tailwind's own docs disclaim the Play CDN build for production use; this repo has no Tailwind installed at all | Part 9 BLOCKER #1 | Prevents shipping an unpurged, runtime-compiled stylesheet and a console warning on every page load |
| 12 | body `## Assets` | CDN Iconify runtime script (`code.iconify.design`) | Removed; replaced with "locally-installed icon package (e.g. lucide-react)" guidance | Third-party runtime script is a CSP/performance/supply-chain risk with no installed dependency backing it in this repo | Part 9 BLOCKER #2 | Prevents an unnecessary external network dependency on the production marketing page |
| 13 | body `## Assets` | Remote `<link>` Google Fonts request | Changed to `next/font/google` build-time self-hosting | Avoids a render-blocking third-party request; idiomatic for this Next.js 16 codebase | Part 9 IMPORTANT finding | Low — swaps a `<link>` tag for a `next/font` import, same visual font |
| 14 | body `## Navigation` / `## Components`, "mobile is hinted via high-contrast buttons" | Not an implementable specification — no real disclosure/menu pattern, no ARIA state, no touch-target size | Replaced with a concrete accessible pattern requirement (disclosure with `aria-expanded`, focus trap, Escape-to-close, 44×44px targets) | Original wording could not be built as accessible navigation as literally written | Part 10 FAIL finding | Requires actual mobile-nav component design/build work — the largest implementation-impact item in this diff |
| 15 | Whole document — Global Navigation, Trial CTA | No CTA destination specified | Added explicit rule: Trial CTA must route to trial signup only, never a live checkout/billing path | Billing is intentionally disabled in production (`NEXT_PUBLIC_BILLING_ENABLED=false`, Hard Constraint #4 in `CLAUDE.md`) | Part 3 DEPLOYED BUT GATED finding | Prevents accidentally wiring a marketing CTA to a disabled billing flow |
| 16 | Whole document — no Migration section | Migration/switching content entirely absent, but is a required mission focus area | Added an explicit "NOT YET AUTHORED" placeholder pointing to the gap analysis, rather than inventing copy | Nothing to normalize — content doesn't exist yet | Part 4 | Signals to product/content owners that this section needs to be authored from real import capability (parts/CSV only today) before publishing |
| 17 | Whole document — no ROI/calculator section | Time-savings/ROI calculator entirely absent | Added an explicit "NOT YET AUTHORED" placeholder with the required safeguards pre-specified for whenever it is built | Nothing to normalize — content doesn't exist yet | Part 5 | Same as above — prevents a future author from skipping the required disclaimer/editable-input safeguards |
| 18 | Whole document — no Competitive Comparison section | Comparison content entirely absent from this spec (though a similar table already exists in the live `app/portal/page.tsx`) | Added an explicit "NOT YET AUTHORED" placeholder, with guidance to use generic "Traditional shop software" framing and maturity labels instead of the live page's current boolean checkmarks | Nothing to normalize in this spec; flagged the adjacent live-page pattern for future revision | Part 6 | No change to DESIGN_VERIFIED.md structure; a note for whoever authors this section next |
| 19 | Whole document — Logo | `components/brand/RedlineD1Logo.tsx` (the expected asset path) does not exist; real asset (`lib/logo.ts`) is a single raster PNG with no variants | Documented the gap explicitly in DESIGN_VERIFIED.md's new Logo section, recommended building a proper SVG/component logo system | Spec's own asset requirements (full/compact/monochrome/tagline variants) cannot be met by a single embedded PNG | Part 7 | Out of scope for this docs pass; flagged as a pre-launch build item |
| 20 | Whole document — Do's and Don'ts | No explicit rule against Sapelee or fake testimonials in public copy (though neither was actually present as a violation) | Added explicit preventive rules for both | Nothing in the source spec violated these, but the mission specifically asked to guard against regression | Part 3 (Sapelee correctly absent; testimonials correctly absent, per prior `app/portal/page.tsx` commits removing them) | None — preventive documentation only |

## Items intentionally left unchanged

- Core color values (`primary #B42318`, `surface-bg #FAFAFA`, `surface-dark
  #171717`, `surface-white #FFFFFF`, `border-light #E5E5E5`) — exact brand
  match, no change needed.
- Corner radius scale, spacing scale, elevation/shadow values — no issues
  found.
- The Pricing/Future Roadmap's current/rollout/future labeling pattern —
  already correct, preserved as-is.
- The restrained use of brand red (accent-only, two-CTA hero pattern) — no
  issues found.
- Sapelee absence and testimonial absence — both already correct in the
  source; no content needed removing.
